# FinPay Client

This workspace will contain the React 19 and Vite application. Increment 1 reserves the boundary
only; UI dependencies and application code will be introduced when the frontend feature begins.
